<h1>Voici le récapitulatif de votre réservation</h1>

<div>
  <div>
    <strong>Date de début:</strong>
    <%= @reservation.start_date.strftime("%d/%m/%Y") %>
  </div>
  <div>
    <strong>Date de fin:</strong>
    <%= @reservation.end_date.strftime("%d/%m/%Y") %>
  </div>
  <div>
    <strong>Prix journalier:</strong>
    <%= @reservation.item.price %> €
  </div>
  <div>
    <strong>Durée total:</strong>
    <%= (@reservation.end_date - @reservation.start_date).to_i %> Jours
  </div>
  <div>
    <strong>Votre total:</strong>
    <%= @reservation.total_price %> €
  </div>


<style>
  .reservation-summary {
    border: 1px solid #ccc;
    padding: 20px;
    margin-top: 20px;
  }

  .reserve-button {
    background-color: red;
    color: white;
    border: none;
    padding: 10px 20px;
    cursor: pointer;
  }

  .reserve-button:hover {
    background-color: darkred;
  }
</style>
