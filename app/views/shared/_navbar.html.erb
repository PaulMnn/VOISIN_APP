<div class="navbar navbar-expand-sm navbar-light navbar-lewagon sticky-top">
  <div class="container-fluid">
    <%= link_to items_path, class: "navbar-brand" do %>
      <%= image_tag "logo.jpg" %>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

      <form class="d-flex mx-auto" role="search" action="<%= items_path %>">
        <input class="form-control me-2 rounded-5" type="search" name="query" placeholder="votre recherche" aria-label="Search">
        <button class="btn btn-primary rounded-5" type="submit">valider</button>
      </form>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item active searchbar">
        <%= link_to "Ajouter un produit", new_item_path, class: "nav-link mx-5" %>
        </li>
        <li class="nav-item">
          <%# <%= link_to "My reservation", reservations_path, class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <%= image_tag "logo.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <% if current_user == nil %>
              <%= link_to "Connexion", new_user_session_path, class: "dropdown-item" %>
            <% else %>
              <%= link_to "Déconnexion",  destroy_user_session_path, class: "dropdown-item", data: {turbo_method: :delete} %>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</div>
